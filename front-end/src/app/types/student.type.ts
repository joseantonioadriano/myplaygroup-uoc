export default class Student {
    constructor(public id: number = 0, public dni: string, public name: string, public surname: string, public idResponsable1: number, public idResponsable2: number, public idSchool: number, public genre: number, public dateBirth: string ) {}
}